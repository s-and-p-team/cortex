# ConfigMaps required for AuthBridge webhook injection
# Apply these to any namespace where you want to use AuthBridge with the webhook
#
# Usage:
#   kubectl apply -f configmaps-webhook.yaml -n <your-namespace>
#
# Note: Update the namespace in metadata if applying to a different namespace

---
# keycloak-admin-secret - Keycloak admin credentials for client registration
# Replace the placeholder values with your actual admin credentials.
apiVersion: v1
kind: Secret
metadata:
  name: keycloak-admin-secret
  namespace: team1
type: Opaque
stringData:
  KEYCLOAK_ADMIN_USERNAME: "admin"
  KEYCLOAK_ADMIN_PASSWORD: "admin"

---
# authbridge-config ConfigMap - Unified config for client-registration, authbridge, and telemetry-processor
#
# client-registration reads: KEYCLOAK_URL, KEYCLOAK_REALM, PLATFORM_CLIENT_IDS
# authbridge reads: KEYCLOAK_URL, KEYCLOAK_REALM (to derive TOKEN_URL/ISSUER), TOKEN_URL, ISSUER,
#                   DEFAULT_OUTBOUND_POLICY
# telemetry-processor: LLM provider registry is built-in (no configmap entry needed)
apiVersion: v1
kind: ConfigMap
metadata:
  name: authbridge-config
  namespace: team1
data:
  KEYCLOAK_URL: "http://keycloak-service.keycloak.svc:8080"
  KEYCLOAK_REALM: "kagenti"
  # TOKEN_URL: Optional. Auto-derived from KEYCLOAK_URL + KEYCLOAK_REALM if not set.
  # Only set explicitly when the token endpoint differs from the standard Keycloak path.
  # TOKEN_URL: "http://keycloak-service.keycloak.svc:8080/realms/kagenti/protocol/openid-connect/token"
  # ISSUER: Optional when KEYCLOAK_URL points to the frontend. Auto-derived from
  # KEYCLOAK_URL + KEYCLOAK_REALM if not set. Set explicitly when the internal
  # KEYCLOAK_URL differs from the frontend URL that appears in token "iss" claims
  # (split-horizon DNS).
  ISSUER: "http://keycloak.localtest.me:8080/realms/kagenti"
  # Audience validation is automatic — uses CLIENT_ID from /shared/client-id.txt
  # (written by client-registration or operator). No manual configuration needed.
  # PLATFORM_CLIENT_IDS: Comma-separated list of Keycloak client IDs that should
  # receive the agent's audience scope. Default: "kagenti"
  # PLATFORM_CLIENT_IDS: "kagenti"
  # DEFAULT_OUTBOUND_POLICY: Controls behavior for outbound requests that don't
  # match any route in authproxy-routes. Default is "passthrough" (skip exchange).
  # Set to "exchange" to restore legacy behavior (exchange all outbound traffic).
  # DEFAULT_OUTBOUND_POLICY: "passthrough"

---
# authproxy-routes ConfigMap - Per-target token exchange routes
#
# Defines which outbound hosts should get token exchange. All other hosts pass
# through unchanged when DEFAULT_OUTBOUND_POLICY is "passthrough" (the default).
#
# This ConfigMap is required for outbound token exchange to work. Without it,
# no outbound token exchange occurs (all traffic passes through). Each route
# specifies the target_audience and token_scopes for a given host.
apiVersion: v1
kind: ConfigMap
metadata:
  name: authproxy-routes
  namespace: team1
data:
  routes.yaml: |
    - host: "auth-target-service"
      target_audience: "auth-target"
      token_scopes: "openid auth-target-aud"

---
# spiffe-helper-config ConfigMap - Used by spiffe-helper container (SPIRE mode only)
apiVersion: v1
kind: ConfigMap
metadata:
  name: spiffe-helper-config
  namespace: team1
data:
  helper.conf: |
    agent_address = "/spiffe-workload-api/spire-agent.sock"
    cmd = ""
    cmd_args = ""
    cert_dir = "/opt"
    renew_signal = ""
    svid_file_name = "svid.pem"
    svid_key_file_name = "svid_key.pem"
    svid_bundle_file_name = "svid_bundle.pem"
    jwt_svids = [{jwt_audience="kagenti", jwt_svid_file_name="jwt_svid.token"}]

---
# envoy-config ConfigMap - Envoy proxy configuration for traffic interception
apiVersion: v1
kind: ConfigMap
metadata:
  name: envoy-config
  namespace: team1
data:
  envoy.yaml: |
    admin:
      address:
        socket_address:
          protocol: TCP
          address: 127.0.0.1
          port_value: 9901

    static_resources:
      listeners:
      - name: outbound_listener
        address:
          socket_address:
            protocol: TCP
            address: 0.0.0.0
            port_value: 15123
        listener_filters:
        - name: envoy.filters.listener.original_dst
          typed_config:
            "@type": type.googleapis.com/envoy.extensions.filters.listener.original_dst.v3.OriginalDst
        - name: envoy.filters.listener.tls_inspector
          typed_config:
            "@type": type.googleapis.com/envoy.extensions.filters.listener.tls_inspector.v3.TlsInspector
        filter_chains:
        # TLS passthrough: forward HTTPS traffic as-is via TCP proxy
        - filter_chain_match:
            transport_protocol: tls
          filters:
          - name: envoy.filters.network.tcp_proxy
            typed_config:
              "@type": type.googleapis.com/envoy.extensions.filters.network.tcp_proxy.v3.TcpProxy
              stat_prefix: outbound_tls_passthrough
              cluster: original_destination
        # Plaintext HTTP: inspect and process via ext_proc
        - filter_chain_match:
            transport_protocol: raw_buffer
          filters:
          - name: envoy.filters.network.http_connection_manager
            typed_config:
              "@type": type.googleapis.com/envoy.extensions.filters.network.http_connection_manager.v3.HttpConnectionManager
              stat_prefix: outbound_http
              codec_type: AUTO
              route_config:
                name: outbound_routes
                virtual_hosts:
                - name: catch_all
                  domains: ["*"]
                  routes:
                  - match:
                      prefix: "/"
                    route:
                      cluster: original_destination
              http_filters:
              - name: envoy.filters.http.ext_proc
                typed_config:
                  "@type": type.googleapis.com/envoy.extensions.filters.http.ext_proc.v3.ExternalProcessor
                  grpc_service:
                    envoy_grpc:
                      cluster_name: ext_proc_cluster
                    timeout: 30s
                  allow_mode_override: true
                  processing_mode:
                    request_header_mode: SEND
                    response_header_mode: SEND
                    request_body_mode: NONE
                    response_body_mode: NONE
              - name: envoy.filters.http.ext_proc
                typed_config:
                  "@type": type.googleapis.com/envoy.extensions.filters.http.ext_proc.v3.ExternalProcessor
                  failure_mode_allow: true
                  allow_mode_override: true
                  grpc_service:
                    envoy_grpc:
                      cluster_name: telemetry_proc_cluster
                    timeout: 30s
                  processing_mode:
                    request_header_mode: SEND
                    response_header_mode: SKIP
                    request_body_mode: NONE
                    response_body_mode: NONE
              - name: envoy.filters.http.router
                typed_config:
                  "@type": type.googleapis.com/envoy.extensions.filters.http.router.v3.Router

      - name: inbound_listener
        address:
          socket_address:
            protocol: TCP
            address: 0.0.0.0
            port_value: 15124
        listener_filters:
        - name: envoy.filters.listener.original_dst
          typed_config:
            "@type": type.googleapis.com/envoy.extensions.filters.listener.original_dst.v3.OriginalDst
        filter_chains:
        - filters:
          - name: envoy.filters.network.http_connection_manager
            typed_config:
              "@type": type.googleapis.com/envoy.extensions.filters.network.http_connection_manager.v3.HttpConnectionManager
              stat_prefix: inbound_http
              codec_type: AUTO
              route_config:
                name: inbound_routes
                virtual_hosts:
                - name: local_app
                  domains: ["*"]
                  routes:
                  - match:
                      prefix: "/"
                    route:
                      cluster: original_destination
              http_filters:
              - name: envoy.filters.http.lua
                typed_config:
                  "@type": type.googleapis.com/envoy.extensions.filters.http.lua.v3.Lua
                  inline_code: |
                    function envoy_on_request(request_handle)
                      request_handle:headers():replace("x-authbridge-direction", "inbound")
                      request_handle:headers():replace("x-telemetry-direction", "inbound")
                      local addr = request_handle:streamInfo():downstreamDirectRemoteAddress()
                      if addr ~= nil then
                        request_handle:headers():replace("x-source-address", addr)
                      end
                    end
              - name: envoy.filters.http.ext_proc
                typed_config:
                  "@type": type.googleapis.com/envoy.extensions.filters.http.ext_proc.v3.ExternalProcessor
                  allow_mode_override: true
                  grpc_service:
                    envoy_grpc:
                      cluster_name: ext_proc_cluster
                    timeout: 30s
                  allow_mode_override: true
                  processing_mode:
                    request_header_mode: SEND
                    response_header_mode: SEND
                    request_body_mode: NONE
                    response_body_mode: NONE
              - name: envoy.filters.http.ext_proc
                typed_config:
                  "@type": type.googleapis.com/envoy.extensions.filters.http.ext_proc.v3.ExternalProcessor
                  failure_mode_allow: true
                  allow_mode_override: true
                  grpc_service:
                    envoy_grpc:
                      cluster_name: telemetry_proc_cluster
                    timeout: 30s
                  processing_mode:
                    request_header_mode: SEND
                    response_header_mode: SKIP
                    request_body_mode: NONE
                    response_body_mode: NONE
              - name: envoy.filters.http.router
                typed_config:
                  "@type": type.googleapis.com/envoy.extensions.filters.http.router.v3.Router

      clusters:
      - name: original_destination
        connect_timeout: 30s
        type: ORIGINAL_DST
        lb_policy: CLUSTER_PROVIDED
        original_dst_lb_config:
          use_http_header: false

      - name: ext_proc_cluster
        connect_timeout: 5s
        type: STATIC
        lb_policy: ROUND_ROBIN
        http2_protocol_options: {}
        load_assignment:
          cluster_name: ext_proc_cluster
          endpoints:
          - lb_endpoints:
            - endpoint:
                address:
                  socket_address:
                    address: 127.0.0.1
                    port_value: 9090

      - name: telemetry_proc_cluster
        connect_timeout: 5s
        type: STATIC
        lb_policy: ROUND_ROBIN
        http2_protocol_options: {}
        load_assignment:
          cluster_name: telemetry_proc_cluster
          endpoints:
          - lb_endpoints:
            - endpoint:
                address:
                  socket_address:
                    address: 127.0.0.1
                    port_value: 9091
